{
  "name": "commercial-landings",
  "private": true,
  "packageManager": "pnpm@9.15.4",
  "scripts": {
    "dev": "pnpm --filter showcase dev",
    "build": "pnpm -r --filter \"./apps/**\" build",
    "lint": "pnpm -r --filter \"./apps/**\" lint",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "sync": "node scripts/portfolio-sync.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "eslint": "^9.22.0",
    "prettier": "^3.5.3",
    "typescript": "^5.8.2"
  }
}
